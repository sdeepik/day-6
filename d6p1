#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    if (n == 0) {
        printf("\n");
        return 0;
    }

    int a[n];  
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < n; i++) {
        printf("%d", a[i]);
        if (i < n - 1) {  
            printf(" ");
        }
    }
    printf("\n");

    return 0;
}
